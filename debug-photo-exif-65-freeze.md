# [OPEN] photo-exif-65-freeze

## 问题描述
- 页面：`src/pages/photo-exif/`
- 现象：上传数量提升到 `65` 张后，页面再次卡死。
- 当前策略：重新开启运行时调试，只做插桩和证据收集，暂不修改业务逻辑。

## 复现步骤
1. 打开 `photo-exif`
2. 上传约 `65` 张图片
3. 观察页面是否在导入后或后续交互中卡死

## 假设
1. 导入阶段存在同步 CPU 重任务，例如 EXIF 解析、缩略图生成或对象 URL 创建在主线程堆积。
2. 列表渲染规模放大后，左侧图片列表或批量预览在 65 张时触发明显的渲染瓶颈。
3. 某个 `useMemo` / `useEffect` / `setState` 链路在大数据量下放大为更新风暴。
4. 地图、统计面板或其它无关区域在大量图片导入后被重复刷新，导致整体卡死。
5. 卡死与图片本身大小或解码失败路径有关，数量只是放大器，不是唯一根因。

## 当前状态
- 状态：已完成修复，等待用户验证
- 业务修复：已完成第一版
- 插桩：保留中，用于对比验证

## 关键证据
- `65` 张 `2.1MB` 真实 JPEG：整批导入约 `2301.7ms`，其中单张预览生成约 `30-45ms`。
- `65` 张 `5.4MB` 真实 JPEG：整批导入约 `3805.7ms`，其中单张预览生成约 `49-67ms`。
- 同一批 `5.4MB` JPEG 在修复后：整批导入约 `136.3ms`。
- `PhotoListPanel` 在导入完成后只提交 `2` 次，不符合列表渲染风暴特征。
- `ExifReader.load(file)` 大多在 `1-3ms`，不是主要瓶颈。

## 结论
1. 假设 1 成立：大批量导入时的主线程重任务主要来自缩略图生成，而不是 EXIF 解析。
2. 假设 2 基本排除：左侧列表提交次数很少，没有出现 65 张下的重渲染风暴。
3. 假设 5 成立：问题与图片内容/体积有关，图片越大，`createPreviewUrl` 越容易把导入阶段拖成“卡死”体感。

## 已实施修复
- 文件：`src/pages/photo-exif/utils.ts`
- 策略：当批量导入达到重负载阈值时，导入阶段直接使用原文件 `objectURL` 作为预览，跳过同步 `canvas` 缩略图生成。
- 当前阈值：
  - 批量数量 `>= 64`
  - 或总字节数 `>= 120MB`

## 待用户验证
1. 重新进入 `photo-exif`
2. 上传约 `65` 张真实图片
3. 观察导入阶段是否还会卡死
4. 如需进一步确认，可继续切到“批量处理”输入改名规则
